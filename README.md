# Generic-File-Based-Database-Management-System
Built a lightweight file-based DBMS in Java using CSV files for table storage, supporting schema-driven table creation and full create, insert, read, and delete operations via file I/O. 

Implemented on-demand Java class generation with runtime compilation and dynamic class loading.
